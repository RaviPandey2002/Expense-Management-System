services:
  - type: static
    name: expense-management-system-frontend-lean
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: client/build
    pullRequestPreviewsEnabled: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
